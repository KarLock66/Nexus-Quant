{
  "name": "@nexus/core",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain types, constants, and the risk gate chain — the single decision surface",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
