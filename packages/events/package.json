{
  "name": "@nexus/events",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical event names and payload contracts for the Redis event backbone",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@nexus/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
